# API auth (set one)
HE_API_KEY=replace_me
# HE_API_KEYS_JSON=["key1","key2"]

# For local development only (avoid in production)
HE_ALLOW_NO_AUTH=0

# Rate limit per API key per minute
HE_RATE_LIMIT_PER_MIN=120

# Stripe connector
STRIPE_API_KEY=sk_live_or_test_key
